# Dependencias DIRECTAS (las que el código importa explícitamente). requirements.txt es
# el lock compilado a partir de este archivo (`pip-compile requirements.in`), e incluye
# además todas las transitivas resueltas (chromadb en particular arrastra bastantes:
# kubernetes, onnxruntime, opentelemetry-*, etc. — eso es normal y esperado, no hay que
# añadirlas aquí a mano).
fastapi==0.136.3
uvicorn==0.49.0
openai==2.41.0
chromadb==1.5.9
pydantic==2.13.4
email-validator==2.3.0
APScheduler==3.11.3
google-api-python-client==2.197.0
google-auth==2.53.0
python-dotenv==1.2.2
requests==2.34.2
httpx==0.28.1
starlette==1.2.1
PyPDF2==3.0.1
